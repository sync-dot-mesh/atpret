//! `atpret` — the composition root. Wires the engine, platform
//! invocation, and CLI-parsed config together.
//!
//! Skeleton only, ahead of real implementation.

fn main() {
    println!("atpret — skeleton only, no real invocation implemented yet.");
}
